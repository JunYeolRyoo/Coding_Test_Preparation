<img width="1001" height="753" alt="image" src="https://github.com/user-attachments/assets/c752a287-0e96-4420-b44d-7c6e2e5d764f" />

``` python
# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution:
    def pathSum(self, root: Optional[TreeNode], targetSum: int) -> List[List[int]]:
        res = []

        def rec(root,li,cursum):
            if root == None: return None

            li.append(root.val)
            l = rec(root.left,li,cursum+root.val)
            r = rec(root.right,li,cursum+root.val)

            if root.left == None and root.right == None: # Reached the leaf node
                if cursum + root.val == targetSum:
                    res.append(li.copy())
            li.pop()

        rec(root,[],0)
        return res
```

**Time Complexity: O(N)** <br>
**Space Complexity: O(N)**
